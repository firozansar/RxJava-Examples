<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="info.firozansari.rxjava">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="info.firozansari.rxjava.activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="info.firozansari.rxjava.activity.OperatorActivity"
            android:parentActivityName="info.firozansari.rxjava.activity.MainActivity"
            />

        <activity
            android:name="info.firozansari.rxjava.activity.SubjectActivity"
            android:parentActivityName="info.firozansari.rxjava.activity.MainActivity"
            />

        <activity
            android:name="info.firozansari.rxjava.activity.NetworkActivity"
            android:parentActivityName="info.firozansari.rxjava.activity.MainActivity"
            />

        <activity
            android:name="info.firozansari.rxjava.activity.SearchActivity"
            android:parentActivityName="info.firozansari.rxjava.activity.MainActivity"
            />

    </application>

</manifest>